version: "3.1"

rules:
  - rule: Router hybride pour questions FAQ
    steps:
      - intent: demander_faq
      - action: action_hybrid_router

  - rule: Fallback NLU explicite
    steps:
      - intent: nlu_fallback
      - action: utter_default
